# 变更日志

## [2026-09-04] 修复 Codex++ 重启后的面板恢复

- 修复 Windows 使用 `/bin/ps` 导致 Codex 根进程无法枚举的问题。
- 在 CDP 连接断开后重新生成 `--open` 请求，使新 Codex 页面能够再次注入。
- 增加 Windows 后台启动入口，等待现有 CDP `9229` 并在注入器退出后恢复运行。
- 明确复用现有 Taskboard 服务和 `.data` 数据目录，避免不必要的服务停止。

## [2026-09-04] 修复后台注入器日志冲突

- 移除启动脚本对重定向日志文件的生命周期追加写入。
- 保留注入器标准输出和错误输出重定向，避免后台启动时因日志文件被占用而退出。
